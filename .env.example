# Copy this file to .env and fill in the values.
# NEVER commit the real .env (it's in .gitignore).
# This is ONLY for the Python side (crawler + emails), run on the Oracle
# VM or GitHub Actions. Stripe/GitHub keys for the payment flow are set
# separately, as Netlify environment variables (see README.md).

# Resend (resend.com -> API Keys)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx
